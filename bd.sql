SELECT name, verssion
FROM Software;

SELECT description, validity_date
FROM licenses;


SELECT computer_id, operating_system_id
FROM Computers;

SELECT user_id, name
FROM Users;


SELECT os_id, name
FROM OperatingSystems;
